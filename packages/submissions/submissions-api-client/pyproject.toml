[project]
name = "submissions-api-client"
version = "1.0.0"
description = "API client for HMP submissions module"
readme = "README.md"
authors = [
    { name = "Mykola Ratushniak", email = "nicolas.ratushniak@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "shared-kernel",
    "submissions-core",
]

[build-system]
requires = ["uv_build>=0.11.8,<0.12.0"]
build-backend = "uv_build"

[tool.uv.sources]
submissions-core = { workspace = true }
shared-kernel = { workspace = true }
