[project]
name = "education-identity-bridge"
version = "1.0.0"
description = "Education-Identity anti-corruption layer"
readme = "README.md"
authors = [
    { name = "Mykola Ratushniak", email = "nicolas.ratushniak@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "education-core",
    "identity-core",
]

[build-system]
requires = ["uv_build>=0.11.8,<0.12.0"]
build-backend = "uv_build"

[tool.uv.sources]
identity-core = { workspace = true }
education-core = { workspace = true }
