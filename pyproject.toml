[project]
name = "hmp-mono"
version = "2.0.0"
description = "HearMyPaper monorepo"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[tool.uv.workspace]
members = [
    "libs/hmp_core",
    "apps/hmp_manager",
    "apps/hmp_tts",
    "apps/hmp_client",
]

[dependency-groups]
dev = [
    "ty>=0.0.34",
    "ruff>=0.15.12",
]
